services:
  prepare-release-action.lint:
    container_name: prepare-release-action.lint
    build:
      context: .
      dockerfile: ops/action/Dockerfile
      target: lint
    environment:
      - NODE_ENV=lint
    tty: true

  prepare-release-action.test:
    container_name: prepare-release-action.test
    build:
      context: .
      dockerfile: ops/action/Dockerfile
      target: test
    depends_on:
      prepare-release-action.lint:
        condition: service_completed_successfully
    environment:
      - NODE_ENV=test
    tty: true

  prepare-release-action.app:
    container_name: prepare-release-action.app
    build:
      context: .
      dockerfile: ops/action/Dockerfile
      target: runtime
    depends_on:
      prepare-release-action.test:
        condition: service_completed_successfully
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"

  prepare-release-action.dev:
    container_name: prepare-release-action.dev
    image: node:24-alpine
    working_dir: /workspace
    volumes:
      - ./:/workspace:cached
      - /workspace/node_modules
    command: sh -c "npm install && npm run start"
    ports:
      - "3000:3000"
